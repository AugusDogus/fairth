{
  "name": "@fairth/android-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && bun build src/main.ts src/healthcheck.ts --target bun --outdir dist --sourcemap=external",
    "start": "bun dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fairth/android-rpc": "workspace:*",
    "@trpc/server": "^11.5.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
